{
  "name": "speedway-almanac",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:up": "docker compose up -d"
  },
  "dependencies": {
    "hono": "^4.10.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
